[pytest]
addopts = -ra -q --alluredir=artifacts/allure-results
pythonpath = . src
markers =
    ui: UI tests using Playwright
    smoke: High-priority smoke coverage
    regression: Full regression coverage
    flaky: Tests known to be flaky or unstable
